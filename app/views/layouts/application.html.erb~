<!DOCTYPE html>
<html>
<head>
  <title>Sistema de Gestión de Llaves - Los Bertas</title>
<%= favicon_link_tag 'favicon.ico' %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
 
 <%=render "layouts/stylesheets"%>
 
</head>
<body>
	<div class="container">
<div>
	<div id="user_nav">
			
		<%if user_signed_in? %>	

<table>
<td>		
		
			Logueado como
				<strong><u>
					<%= link_to current_user.email,edit_user_registration_path%>
				</strong></u>
			No eres tu?
				<strong>
					<%= link_to "Cerrar Sesión", destroy_user_session_path, :method => :delete%>
				</strong>
</td>
<td>
			Hola!
				<strong> 
					<%= current_user.persona.nombre%>
					<%= current_user.persona.apellido%>
				</strong>

		<%else%>
					
		<%end%>
</td>
</table>

				<%if current_user.try(:admin?)%>
						<H3>
 							 <%=image_tag("admin.png")%> SuperUser:<strong> <%= current_user.email%></strong>
 						</H3>	 
				<%end%>
					
		<%= link_to(image_tag("inicio.png"), :root)%>
		
<section class="round">
<%= yield %>
			<%=render "layouts/footer"%>
	</section>	
	</div>
</body>
</html>
